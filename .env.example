# Shadow Level — Environment Variables
# Copy this file to .env and fill in the values.
# Currently no external services are required — all data is stored in localStorage.

# If you add a backend in the future, configure it here:
# VITE_API_URL=http://localhost:3000/api
